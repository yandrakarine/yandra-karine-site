import styled from 'styled-components';

import { Container } from '../common';

export const ThemeIcon = styled.img`
  width: 1.3rem;
`;
export const FixIcons = styled(Container)`
  justify-content: center;
  align-items: center;
  height: 100%;
  font-size: 15;
  padding-right: 2;
`;
